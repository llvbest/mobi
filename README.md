Реализовать простой новостной сайт с двумя страницами: список и просмотр новости.

Основные требования
 - реализация должна быть без использования фреймворков и библиотек

Данные:
 - новости: id, категория, название, текст, url картинки, дата публикации
 - категории: можно хранить в коде

Дополнительно:
 - сделать возможность сортировать новости по категории и дате публикации
 - для добавления новостей сделать генератор с консольным или веб интерфейсом
 - в качесте содержания новостей можно использовать Lorem Ipsum или что угодно другое
 - предусмотреть возможность быстрого добавления интерфейса для редактирования новостей

Нагрузка:
 - количество новостей в списке – до 1 000 000
 - устойчивость – 1000 запросов к списку новостей в минуту
 - время открытия страницы списка новостей < 500 мс

Технологии:
 - бэк: PHP, mysql, memcached
 - фронт: на ваше усмотрение

Проект должен быть на гитхабе и отражать процесс разработки.
В результате — ссылка на гитхаб и развёрнутое демо

________________________________________________________________________
Cсылка на гит - https://github.com/llvbest/mobi
Развернутое демо - http://test.socanalysis.org/

Написаны самописные классы с автозагрузчиками во входном скрипте, дабы разделить функционал 
по шаблонам ООП. Некоторый контроллер (в нем вся выборка с базы), создан класс для работы через
pdo с базой, экранирование и составление с помощью строителя запросы к бд, не допустить инъекций.
Иерархия файлов разделена структурировано по папкам.

База выбрана по заданию mysql, тип таблици news MyiSAM т.к нам в приоритете большая скорость выборки.
навешаны индексы на поля категории и дате публикации, т.к нам нужно фильтровать и сортировать по этим полям.
Также составной индекс категория_дата, поскольку если делать выборку по двум полям тогда
селективность такого индекса и быстрота сокращения по индексу в разы повышается, 
т.к по условию категорий не много и можно хранить в коде (не создавать промежуточные таблици, связи многие ко многим)

Выборка кол-ва новостей и листинг отображения кешируются в memcached по составному ключу.

По условию не сказано о полном пагинаторе новостного каталога, ибо исключить громоздких
выборки со смещением (LIMIT/OFFSET) могут работать очень медленно при больших значениях смещений.
Врят ли в новостном каталоге пользователь долистает до последней страници(возможно скроллинг ajax).
Если требуется полный пагинатор! (с фильтрами и сортировкой), что значительно уменьшит скорость при больших
смещениях offset... так как индекс(или составной в зависимости от фильтров) находится в памяти 
на момент выполнения запроса, такой эффект называется  covering index. 
Тоесть модифицировать выборку с подзапросом и с индекса выбирать ids новостей по фильтрам и сортировкой, 
а далее join с основной таблицей выборку по ids. При у величении кол-ва записей и такие запросы будут терять в скорости
(переход на аналитические субд или хранение пересчитываемых избыточных данных по составным ключам по фильтрам и сортировке).

Дабы скорость загрузки страници была максимальной, в листинге новостей картинки урезаются до нужных размеров
и форматов только картинок (данный функционал может быть заложен), скрипт js подключаются в конце страници.
Возможно применить минимизацию js,css, а также CDN.
Заложена небольшая часть! валидации данных при добавлении новости в веб интерфейсе (акцент на это не делался).

//генерация новостей рендомными данными(Lorem Ipsum) с как с консоли, так и одиночно с веб интерфейса
php generator.php 2>&1 &
http://prntscr.com/thz78h

Реализован функционал удаления, добавления/редактирования(просмотр) новостей, загрузка картинок.
Редактирование/просмотр по нажатию в списке на название http://prntscr.com/thz57e
Сортировка по хедерам таблици.
Фильтрация и сортировка по катеориям и дате, реализован вывод кол-ва найденных и простая листалка страниц
http://prntscr.com/ti00yj

//весь код уникален, ни на гитхабе ни в просторах его нет

1.config file for connect database ./ideal/classes/Config.php
2. dump dor database: dump_db.sql
3. folder uploads chmod(0755)
_----
on native php, mysql, memcached, jquery, bootstrap
    
    

